<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <header class="event-details-page">
      <h1>This is Event Details Page!</h1>
      <a href="/events"><button>Back</button></a>
    </header>
    <div class="event-details-page container">
      <div class="remarks">
        <% event.remarks.forEach((remark)=>{%> <%if(remark._id == remarkid){%>
        <form
          class="remark-form"
          action="/remarks/<%= remark._id%>/edit"
          method="POST"
        >
          <textarea name="title" rows="4" required="true">
<%= remark.title%></textarea
          >
          <input
            type="text"
            name="author"
            value="<%= remark.author%>"
            required="true"
          />
          <button type="submit">POST</button>
        </form>
        <%}%> <% })%>
      </div>
      <div class="event-details">
        <div>
          <div class="details-ctrl">
            <h2>TITLE:</h2>
            <p><%= event.title%></p>
          </div>
          <div class="details-ctrl">
            <h2>SUMMARY:</h2>
            <p><%= event.summary%></p>
          </div>
          <div class="details-ctrl">
            <h2>HOST:</h2>
            <p><%= event.host%></p>
          </div>
          <div class="details-ctrl">
            <h2>START:</h2>
            <p><%= event.start_date%></p>
          </div>
          <div class="details-ctrl">
            <h2>END:</h2>
            <p><%= event.end_date%></p>
          </div>
          <div class="details-ctrl">
            <h2>CATEGORY:</h2>
            <%event.event_category.forEach((c)=>{%>
            <p><%= c.toUpperCase() %></p>
            <%})%>
          </div>
          <div class="details-ctrl">
            <h2>LOCATION:</h2>
            <p><%= event.location%></p>
          </div>
          <div class="details-ctrl">
            <h2>LIKES:</h2>
            <p><%= event.likes%></p>
          </div>
          <div class="details-ctrl">
            <a href="/events/<%= event._id%>/edit"><button>🖊</button></a>
            <a href="/events/<%= event._id%>/delete"><button>🗑</button></a>
            <a href="/events/<%= event._id%>/likes/+"><button>😍</button></a>
            <a href="/events/<%= event._id%>/likes/-"><button>🤯</button></a>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
